export interface Command {
    id: string
    title: string
}

/*
export interface Command {
    id: string
    targets: string[]   // e.g ['workspace']
    condition: { 
        min: number
        max: number
    }
}
*/

